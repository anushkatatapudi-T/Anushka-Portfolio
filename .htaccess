<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /portfolio/

  # Route non-existing files and directories through PHP proxy
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
